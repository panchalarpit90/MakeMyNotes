<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MakeMyNotes" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowBackground">@color/mainBg</item>
        <item name="android:statusBarColor">@color/mainBg</item>
    </style>

    <style name="Theme.MakeMyNotes" parent="Base.Theme.MakeMyNotes" />
</resources>